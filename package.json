{
  "name": "rehype-slots",
  "repository": "marekweb/rehype-slots",
  "license": "MIT",
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "check": "npm run check:lint && npm run check:format",
    "check:lint": "eslint .",
    "check:format": "prettier --check '**/*.{js,ts,md}'",
    "fix": "npm run fix:lint && npm run fix:format",
    "fix:lint": "eslint --fix .",
    "fix:format": "prettier --write '**/*.{js,ts,md}'",
    "build": "npm run build:types && npm run build:bundle",
    "build:types": "tsc --build",
    "build:bundle": "esbuild rehype-slots/rehype-slots.ts --bundle --platform=neutral \"--external:./node_modules/*\" --outfile=dist/rehype-slots/rehype-slots.ts",
    "test": "node --import tsx --test '**/*.test.ts'"
  },
  "exports": "./dist/lib/rehype-slots.js",
  "files": [
    "dist/rehype-slots"
  ],
  "types": "./dist/lib/rehype-slots.d.ts",
    "prettier": {
      "proseWrap": "always"
    },
  "dependencies": {
    "unist-util-flatmap": "^1.0.0"
  },
  "devDependencies": {
    "@types/hast": "^3.0.4",
    "@types/node": "^20.11.17",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "esbuild": "^0.20.0",
    "hastscript": "^9.0.0",
    "prettier": "^3.2.5",
    "rehype-format": "^5.0.0",
    "rehype-parse": "^9.0.0",
    "rehype-stringify": "^10.0.0",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3",
    "unified": "^11.0.4"
  }
}
